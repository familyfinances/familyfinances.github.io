# Site Build Settings
repository: familyfinances/familyfinances.github.io.git
url: "https://familyfinances.study"
future: true

# jekyll-paginate configuration
paginate:            5

# Build settings
markdown:            kramdown
highlighter:         rouge
permalink:           /:title
plugins:             [jekyll-paginate, jekyll-sitemap, jekyll-feed, jekyll-seo-tag]

# Customise atom feed settings (this is where Jekyll-Feed gets configuration information)
title:		           "Family Finances"
description:	       "A collaborative project between the University of York, the London School of Economics, and the Child Poverty Action Group."
author:              "family-finances"

# RSS 2.0 can be used instead of Atom by uncommenting following two lines
#feed:
#  path: rss-feed.xml
